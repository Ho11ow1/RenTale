init 1 python:
    class Flags:
        # === MAIN QUEST FLAGS ===
        class Main:
            MainEvent1 = FlagRef(False)


        # === SIDE QUEST FLAGS ===
        class Side:
            SideEvent1 = FlagRef(False)


        # === CHARACTER FLAGS ===
        class Willow:
            HaveMet = FlagRef(False)


        class Alice:
            HaveMet = FlagRef(False)

